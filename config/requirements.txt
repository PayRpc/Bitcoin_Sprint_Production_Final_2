# Requirements for Bitcoin Exporter
# =================================
# Pinned versions for reproducible builds and security

# HTTP client for RPC calls to Bitcoin Core
requests==2.32.5

# Prometheus client for metrics formatting
prometheus-client==0.22.1
