# Bitcoin Sprint Configuration - ENTERPRISE TIER
# Enterprise tier with maximum performance and security

# Activate Enterprise Tier
TIER=enterprise

# API Configuration
API_HOST=0.0.0.0
API_PORT=9000

# Admin Configuration (Separate port for enterprise)
ADMIN_PORT=9001

# Web Dashboard Configuration
WEB_HOST=0.0.0.0
WEB_PORT=3002

# Bitcoin Node Configuration
BITCOIN_NODE=127.0.0.1:8333
ZMQ_NODE=127.0.0.1:28332

# P2P Configuration
PEER_LISTEN_PORT=8335
PEER_HMAC_SECRET=bitcoin-sprint-enterprise-secret-key-2025-secure

# Security & Authentication
LICENSE_KEY=ENTERPRISE-FULL-FEATURES-ACTIVE
API_KEY=enterprise-api-key-changeme

# Performance Settings (Enterprise Tier)
USE_DIRECT_P2P=true
USE_MEMORY_CHANNEL=true
OPTIMIZE_SYSTEM=true
ENABLE_KERNEL_BYPASS=false

# Rate Limiting (Free Tier)
RATE_LIMIT_REQUESTS_PER_SECOND=1
RATE_LIMIT_REQUESTS_PER_HOUR=1000
CONCURRENT_STREAMS=1
DATA_SIZE_LIMIT_MB=10

# Monitoring (Basic)
ENABLE_PROMETHEUS=true
PROMETHEUS_PORT=9090
ENABLE_ENTROPY_MONITORING=false

# Database (Basic)
DATABASE_TYPE=sqlite
DATABASE_URL=./free_tier.db
ENABLE_PERSISTENCE=true

# Network Configuration
# Ethereum Configuration
ETH_RPC_ENDPOINTS=https://eth.llamarpc.com,https://rpc.ankr.com/eth,https://ethereum.blockpi.network/v1/rpc/public
ETH_WS_ENDPOINTS=wss://eth.llamarpc.com
ETH_TIMEOUT=30
ETH_MAX_CONNECTIONS=4

# Solana Configuration  
SOLANA_RPC_ENDPOINTS=https://api.mainnet-beta.solana.com,https://solana.blockpi.network/v1/rpc/public,https://rpc.ankr.com/solana
SOLANA_WS_ENDPOINTS=wss://solana.blockpi.network/v1/ws/public
SOLANA_TIMEOUT=30
SOLANA_MAX_CONNECTIONS=8

# Connection Settings
MAX_RETRY_ATTEMPTS=3
RETRY_DELAY_SECONDS=2
WS_TIMEOUT=30

# Logging
LOG_LEVEL=info

# Storage Verification Configuration
STORAGE_VERIFICATION_ENABLED=true
DEFAULT_PROTOCOL=ipfs
SUPPORTED_PROTOCOLS=ipfs,filecoin,arweave,custom
MAX_FILE_SIZE_MB=100
VERIFICATION_TIMEOUT_SECONDS=30

# Enterprise Server Configuration
ENTERPRISE_PORT=8443
TLS_CERT_PATH=certs/server.crt
TLS_KEY_PATH=certs/server.key
TLS_ENABLED=true

# Subscription Tiers
FREE_CREDITS_PER_MONTH=100
DEVELOPER_CREDITS_PER_MONTH=1000
PROFESSIONAL_CREDITS_PER_MONTH=50000
ENTERPRISE_CREDITS_PER_MONTH=999999999

# Webhook Configuration
WEBHOOK_TIMEOUT_SECONDS=30
WEBHOOK_MAX_RETRIES=3

# API Keys (for development)
BITCOIN_SPRINT_ENTERPRISE_API_KEY=ent_2a4f3a2974a84fe9a6174a5f
BITCOIN_SPRINT_PRO_API_KEY=pro_1234567890abcdef
BITCOIN_SPRINT_FREE_API_KEY=free_abcdef1234567890
LOG_FILE=logs/free_tier.log
