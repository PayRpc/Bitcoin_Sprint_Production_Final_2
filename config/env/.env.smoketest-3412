# Bitcoin Sprint Smoke Test Configuration (20250907_001555)
# Test ID: c8bd7e14

# Core configuration
TIER=enterprise
LOG_LEVEL=info
RUN_ID=c8bd7e14
RUN_MODE=test
TEST_RUN=true

# API Configuration
API_HOST=127.0.0.1
API_PORT=3412
API_BIND=127.0.0.1:3412
ADMIN_PORT=3413
HEALTH_PORT=3413
PROMETHEUS_PORT=3422

# Optimization settings
RATE_LIMIT_FACTOR=0.25
MAX_CONCURRENT_REQUESTS=50
CONNECTION_TIMEOUT_SEC=15

# ETH endpoints
ETH_HTTP_URL=https://eth-rpc.nethermind.io
ETH_WS_URL=wss://eth-ws.nethermind.io
ETH_TIMEOUT=15s
ETH_MAX_CONNECTIONS=5
ETH_USE_ACCELERATION=true
ETH_RETRY_ATTEMPTS=3

# Solana endpoints
SOL_RPC_URL=https://api.mainnet-beta.solana.com
SOLANA_TIMEOUT=15s
SOLANA_MAX_CONNECTIONS=3
SOL_USE_ACCELERATION=true

# JSON array configurations
ETH_RPC_ENDPOINTS=["https://eth-rpc.nethermind.io","https://gateway.fm/eth/rpc","https://rpc.flashbots.net","https://ethereum.publicnode.com","https://cloudflare-eth.com","https://rpc.ankr.com/eth"]
ETH_WS_ENDPOINTS=["wss://eth-ws.nethermind.io","wss://gateway.fm/eth/ws","wss://ethereum.publicnode.com","wss://rpc.ankr.com/eth/ws"]
SOLANA_RPC_ENDPOINTS=["https://api.mainnet-beta.solana.com","https://rpc.ankr.com/solana"]
SOLANA_WS_ENDPOINTS=["wss://api.mainnet-beta.solana.com","wss://rpc.ankr.com/solana/ws"]

# Sprint Acceleration Layer configuration
ACCELERATION_ENABLED=true
DEDUPLICATION_TIER=ENTERPRISE
CROSS_NETWORK_DEDUP=true
INTELLIGENT_EVICTION=true
NETWORK_SPECIFIC_TTL=true
DEDUP_CAPACITY=20480
ADAPTIVE_OPTIMIZATION=true

# Network-specific acceleration optimizations
ETH_ACCELERATION_TTL=300
ETH_BLOCK_PRIORITY=8
ETH_ENDPOINT_ROTATION_STRATEGY=reliability
ETH_ENDPOINT_MAX_FAILURES=3
ETH_ENDPOINT_COOLDOWN_SEC=60
ETH_ENDPOINT_HEALTH_CHECK_INTERVAL=30
ETH_REQUEST_TIMEOUT=15
SOL_ACCELERATION_TTL=100
SOL_BLOCK_PRIORITY=6

# Latency flattening configuration
LATENCY_FLATTENING_ENABLED=true
PREDICTIVE_CACHING_ENABLED=true
ENDPOINT_CIRCUIT_BREAKER=true
MULTI_PEER_REDUNDANCY=true
PARALLEL_REQUEST_THRESHOLD=200
RESPONSE_VERIFICATION_MODE=full
SMART_BATCHING_ENABLED=true
COMPETITIVE_EDGE_MODE=true

# Ensure we don't use hardcoded ports
HTTP_PORT=3412
BITCOIN_SPRINT_HTTP_PORT=3412
BITCOIN_SPRINT_API_PORT=3412
BITCOIN_SPRINT_PROFILE=smoke
